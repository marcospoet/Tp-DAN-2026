# ============================================================
# AI SERVICE — application.properties
# ============================================================

server.port=8083
spring.application.name=ai-service

# ------------------------------------------------------------
# MONGODB — unica base de datos de este servicio
# Este servicio NO accede a PostgreSQL
# Colecciones: chat_sessions, chat_messages, analytics_cache
# ------------------------------------------------------------
spring.data.mongodb.uri=mongodb://${MONGO_USER:ai_user}:${MONGO_PASSWORD:ai_pass}@${MONGO_HOST:localhost}:27017/ai_db?authSource=ai_db
spring.data.mongodb.auto-index-creation=true

# ------------------------------------------------------------
# RABBITMQ
# Eventos que consume: transaction.created (invalida cache de analytics)
# Eventos que publica: (ninguno por ahora)
# ------------------------------------------------------------
spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=5672
spring.rabbitmq.username=${RABBITMQ_USER:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}

# ------------------------------------------------------------
# EUREKA
# ------------------------------------------------------------
eureka.client.service-url.defaultZone=http://${EUREKA_HOST:localhost}:8761/eureka

# ------------------------------------------------------------
# AI PROVIDERS — claves server-side (no las maneja el frontend)
# Configurar en .env o en variables de entorno del contenedor.
# Proveedor activo: claude | openai | gemini
# ------------------------------------------------------------
ai.provider=${AI_PROVIDER:claude}
ai.claude-api-key=${CLAUDE_API_KEY:}
ai.openai-api-key=${OPENAI_API_KEY:}
ai.gemini-api-key=${GEMINI_API_KEY:}

# ------------------------------------------------------------
# TOOL CALLING — agente con function calling sobre transaction-service
# ------------------------------------------------------------
ai.tools-enabled=${AI_TOOLS_ENABLED:true}
ai.transaction-service-base-url=http://${TRANSACTION_HOST:localhost}:8082

# ------------------------------------------------------------
# AUTH SERVICE — resolucion interna de API keys del usuario (cifradas en BD)
# ------------------------------------------------------------
ai.auth-service-base-url=http://${AUTH_HOST:localhost}:8081
ai.internal-api-secret=${INTERNAL_API_SECRET:}

# ------------------------------------------------------------
# RAG — pgvector (PostgreSQL), schema "ai"
# Knowledge base estática (Markdown/PDF) indexada bajo demanda
# ------------------------------------------------------------
spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST:localhost}:5432/${POSTGRES_DB:pesito}?currentSchema=ai,public
spring.datasource.username=${AI_DB_USER:ai_db_user}
spring.datasource.password=${AI_DB_PASSWORD:ai_pass}
spring.flyway.schemas=ai
spring.flyway.default-schema=ai

# ------------------------------------------------------------
# CORS — permitir requests del frontend Next.js
# ------------------------------------------------------------
spring.mvc.cors.allowed-origins=http://localhost:3001,http://localhost:3000

# ------------------------------------------------------------
# CACHE — API keys de usuario (resueltas via auth-service interno).
# TTL corto: un cambio de keys en Settings tarda como mucho 60s en
# reflejarse en las llamadas de IA.
# ------------------------------------------------------------
spring.cache.type=caffeine
spring.cache.cache-names=user-api-keys
spring.cache.caffeine.spec=maximumSize=10000,expireAfterWrite=60s

# ------------------------------------------------------------
# RESILIENCE4J — Proteccion de llamadas a proveedores de IA externos
# (Claude, OpenAI, Gemini). Si el proveedor falla repetidamente, el
# circuito se abre y se devuelve un fallback en lugar de seguir
# esperando timeouts.
# ------------------------------------------------------------
resilience4j.circuitbreaker.instances.ai-provider.sliding-window-size=10
resilience4j.circuitbreaker.instances.ai-provider.minimum-number-of-calls=5
resilience4j.circuitbreaker.instances.ai-provider.failure-rate-threshold=50
resilience4j.circuitbreaker.instances.ai-provider.wait-duration-in-open-state=20s
resilience4j.circuitbreaker.instances.ai-provider.permitted-number-of-calls-in-half-open-state=3
resilience4j.circuitbreaker.instances.ai-provider.register-health-indicator=true

resilience4j.retry.instances.ai-provider.max-attempts=2
resilience4j.retry.instances.ai-provider.wait-duration=500ms
resilience4j.retry.instances.ai-provider.retry-exceptions[0]=org.springframework.web.reactive.function.client.WebClientException
resilience4j.retry.instances.ai-provider.retry-exceptions[1]=java.io.IOException
resilience4j.retry.instances.ai-provider.ignore-exceptions[0]=io.github.resilience4j.circuitbreaker.CallNotPermittedException

# ------------------------------------------------------------
# ACTUATOR
# ------------------------------------------------------------
management.endpoints.web.exposure.include=health,info,prometheus,circuitbreakers,circuitbreakerevents
management.endpoint.health.show-details=always
management.health.circuitbreakers.enabled=true
